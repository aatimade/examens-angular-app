import { DecisionLabelPipe } from './decision-label-pipe';

describe('DecisionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DecisionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
